[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "runner-air-planner"
version = "0.1.0"
description = "Runner's Clean Air Planner core package"
readme = "README.md"
requires-python = ">=3.11"
authors = [
    {name = "Runner Air Planner Team"}
]
dependencies = []

[project.optional-dependencies]
dev = []

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"
